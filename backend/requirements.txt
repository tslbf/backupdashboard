fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic-settings>=2.4
apscheduler>=3.10,<4
httpx>=0.27

# SQL Server driver — the app DB in production, and the legacy BackupReporting
# table the PowerShell scripts filled. Requires "ODBC Driver 18 for SQL Server"
# on the host.
pyodbc>=5.1

# Windows ships no IANA tz database; zoneinfo needs this to resolve
# Europe/London, which is the whole basis of the report-day math.
tzdata>=2024.1
